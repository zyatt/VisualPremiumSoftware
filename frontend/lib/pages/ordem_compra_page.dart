import 'package:flutter/material.dart';

class OrdemCompraPage extends StatelessWidget {
  const OrdemCompraPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('OrdemCompraPage')),
      body: const Center(child: Text('OrdemCompraPage — em construção')),
    );
  }
}