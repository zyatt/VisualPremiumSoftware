import 'package:flutter/material.dart';

class ControleEstoquePage extends StatelessWidget {
  const ControleEstoquePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('ControleEstoquePage')),
      body: const Center(child: Text('ControleEstoquePage — em construção')),
    );
  }
}