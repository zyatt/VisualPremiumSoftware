import 'package:flutter/material.dart';

class RelatorioOSPage extends StatelessWidget {
  const RelatorioOSPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Relatório OS')),
      body: const Center(child: Text('RelatorioOSPage — em construção')),
    );
  }
}